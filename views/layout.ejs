<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <style>
      /* Styling for Sidebar */
      .sidebar {
        background: linear-gradient(135deg, #f8dbdb, #a00707);
        box-shadow: 3px 0 5px rgba(0, 0, 0, 0.2);
        color: white;
        padding-top: 1.5rem;
      }

      /* Sidebar Header */
      .sidebar-header {
        text-align: center;
        padding: 1rem;
      }
      .sidebar-header img {
        width: 80px; /* Adjust as needed */
        height: 80px; /* Adjust as needed */
        border-radius: 50%; /* Circular image */
        object-fit: cover; /* Maintain aspect ratio */
      }
      .sidebar-header h5,
      .sidebar-header p {
        color: #fff;
        margin: 0;
        font-weight: 700;
      }

      /* Navigation Links */
      .nav-link {
        color: #dcdcdc;
        font-weight: bold;
        padding: 10px 20px;
        margin-bottom: 0.5rem;
        border-radius: 5px;
      }
      .nav-link:hover {
        background-color: rgba(255, 255, 255, 0.1);
        color: #fff;
      }
      .nav-link.active {
        color: #a00707;
        background-color: #ffffff48;
        border-radius: 5px;
      }
      .nav-link i {
        margin-right: 8px;
      }

      /* Custom style for the log-out box */
      .logout-box {
        background-color: #ffffff;
        border: 1px solid #ffffff;
        border-radius: 20px;
        color: #721c24;
        margin-top: auto;
        margin-bottom: 1rem;
        padding: 5px; /* Reduced padding */
      }
      .logout-box a {
        color: inherit;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.9rem; /* Smaller font size */
        padding: 5px 10px; /* Adjusted padding */
      }
      .logout-box a:hover {
        color: #a94442;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <!-- Sidebar -->
        <div class="col-12 col-md-3 col-lg-2 sidebar d-flex flex-column" style="min-height: 100vh;">
          <div class="sidebar-sticky flex-grow-1">
            <div class="sidebar-header">
              <img src="/LogoAbout.png" alt="Logo" class="img-fluid mb-2">
            </div>
            <ul class="nav flex-column">
              <li class="nav-item">
                <a class="nav-link active" href="/cms/admin">
                  <i class="bi bi-house-door-fill"></i> Dashboard
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/cms/tasks">
                  <i class="bi bi-clipboard"></i> Tasks
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/cms/submission">
                  <i class="bi bi-upload"></i> Submission
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/cms/certificate">
                  <i class="bi bi-file-earmark-text"></i> Certificate
                </a>
              </li>
            </ul>
          </div>
          
          <!-- Log Out at the bottom with red box -->
          <div class="logout-box text-center">
            <a class="nav-link fw-bold" href="/logout">
              <i class="bi bi-box-arrow-right"></i> Log Out
            </a>
          </div>
        </div>

        <!-- Main Content -->
        <div class="col-12 col-md-9 col-lg-10">
          <div class="py-3">
            <h2><%= title %></h2>
          </div>

          <!-- Dynamic content will go here -->
          <%- body %>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
